const authRouter = require("./auth");
const usersRouter = require("./users");
const genericRouter = require("./generic");

module.exports = {
      authRouter,
      usersRouter,
      genericRouter,
};
